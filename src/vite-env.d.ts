/**
 * Vite & Svelte Ambient Type Declarations.
 * What: Ambient type references for Vite client asset imports (CSS, static assets) and Svelte component modules.
 * When: Loaded globally by TypeScript compiler during project type checking and IDE language server analysis.
 * Why: Resolves side-effect CSS imports ('ol/ol.css') and Vite client environment types.
 */

/// <reference types="svelte" />
/// <reference types="vite/client" />
